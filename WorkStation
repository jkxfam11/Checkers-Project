** Public Use: **








**Harold's Section:**

So a while back I looked into how make a basic frame, and if we put this code into a main method it should run a window for us.

import java.awt.*;
import javax.swing.*;

public class JFrame_Testing 
{
	public static void main(String[] args) 
	{
		Jframe frame = new JFrame("Starting Window"); //Create the frame
		
		//Set up the frame
		frame.setVisible(true); //Visibility
		frame.setPreferredSize(new Dimension(800,600)); //Create a dimension object to fit into the size of the window
		frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); //Make it closable
		frame.pack(); //Apply size changes

		JLabel label = new JLabel("This is practice code without looking at outside examples.");
		label.setPreferredSize(new Dimension(200,100));
		
		frame.getContentPane().add(label,SwingConstants.CENTER);

	}





**Kevin's Section:**
I think that we need to improt something for drawing panels, graphics, and Jframes, should we use java, like java.awt.* and java.util.*, to draw the 8x8 grid;
Should we make it so that the graphics screen is proportional to the laptop screen or should it be same size because that determies whether or not we need to add variables.
So maybe DrawingPanel a = new DrawingPanel(800,600)? Graphics g=a.getGraphics?
import java.awt.Graphics;
import java.util.*;
public class CheckersMain {
	public static void main(String[] args) {
		DrawingPanel p = new DrawingPanel(400,200);
		Graphics g = p.getGraphics();
		p.setBackground(Color.BLACK);
		g.setColor(Color.YELLOW);

	}
}
public class DrawingPanel{}
